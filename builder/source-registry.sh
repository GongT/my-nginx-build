#!/usr/bin/env bash

NGX_SRC_PATH+=(nginx)
NGX_SRC_REPO+=(nginx/nginx)
NGX_SRC_BRANCH+=(master)

NGX_SRC_PATH+=(modules/echo-nginx-module)
NGX_SRC_REPO+=(openresty/echo-nginx-module)
NGX_SRC_BRANCH+=(master)

NGX_SRC_PATH+=(modules/headers-more-nginx-module)
NGX_SRC_REPO+=(openresty/headers-more-nginx-module)
NGX_SRC_BRANCH+=(master)

NGX_SRC_PATH+=(modules/ngx_cache_purge)
NGX_SRC_REPO+=(FRiCKLE/ngx_cache_purge)
NGX_SRC_BRANCH+=(master)

NGX_SRC_PATH+=(modules/memc-nginx-module)
NGX_SRC_REPO+=(openresty/memc-nginx-module)
NGX_SRC_BRANCH+=(master)

NGX_SRC_PATH+=(modules/ngx_devel_kit)
NGX_SRC_REPO+=(simplresty/ngx_devel_kit)
NGX_SRC_BRANCH+=(master)

NGX_SRC_PATH+=(modules/nginx_cookie_flag_module)
NGX_SRC_REPO+=(AirisX/nginx_cookie_flag_module)
NGX_SRC_BRANCH+=(master)

NGX_SRC_PATH+=(modules/ngx_brotli)
NGX_SRC_REPO+=(google/ngx_brotli)
NGX_SRC_BRANCH+=(master)

NGX_SRC_PATH+=(modules/ngx_brotli/deps/brotli)
NGX_SRC_REPO+=(google/brotli)
NGX_SRC_BRANCH+=(master)

NGX_SRC_PATH+=(modules/nginx_accept_language_module)
NGX_SRC_REPO+=(giom/nginx_accept_language_module)
NGX_SRC_BRANCH+=(master)

NGX_SRC_PATH+=(modules/ngx-fancyindex)
NGX_SRC_REPO+=(aperezdc/ngx-fancyindex)
NGX_SRC_BRANCH+=(master)

NGX_SRC_PATH+=(modules/traffic-accounting-nginx-module)
NGX_SRC_REPO+=(Lax/traffic-accounting-nginx-module)
NGX_SRC_BRANCH+=(master)

NGX_SRC_PATH+=(modules/ngx_log_if)
NGX_SRC_REPO+=(cfsego/ngx_log_if)
NGX_SRC_BRANCH+=(master)

# NGX_SRC_PATH+=(modules/nchan)
# NGX_SRC_REPO+=(slact/nchan)
# NGX_SRC_BRANCH+=(master)

NGX_SRC_PATH+=(modules/ngx_http_geoip2_module)
NGX_SRC_REPO+=(leev/ngx_http_geoip2_module)
NGX_SRC_BRANCH+=(master)

NGX_SRC_PATH+=(modules/nginx-rtmp-module)
NGX_SRC_REPO+=(arut/nginx-rtmp-module)
NGX_SRC_BRANCH+=(master)

NGX_SRC_PATH+=(special-modules/njs)
NGX_SRC_REPO+=(nginx/njs)
NGX_SRC_BRANCH+=(master)

# NGX_SRC_PATH+=(modules/lua-nginx-module)
# NGX_SRC_REPO+=(openresty/lua-nginx-module)
# NGX_SRC_BRANCH+=('@@master')

# NGX_SRC_PATH+=(luajit2)
# NGX_SRC_REPO+=(openresty/luajit2)
# NGX_SRC_BRANCH+=(v2.1-agentzh)

# NGX_SRC_PATH+=(resty/lua-resty-string)
# NGX_SRC_REPO+=(openresty/lua-resty-string)
# NGX_SRC_BRANCH+=(master)

# NGX_SRC_PATH+=(resty/lua-resty-lrucache)
# NGX_SRC_REPO+=(openresty/lua-resty-lrucache)
# NGX_SRC_BRANCH+=(master)

# NGX_SRC_PATH+=(resty/lua-resty-core)
# NGX_SRC_REPO+=(openresty/lua-resty-core)
# NGX_SRC_BRANCH+=(master)

# NGX_SRC_PATH+=(resty/lua-tablepool)
# NGX_SRC_REPO+=(openresty/lua-tablepool)
# NGX_SRC_BRANCH+=(master)

# NGX_SRC_PATH+=(resty/lua-resty-signal)
# NGX_SRC_REPO+=(openresty/lua-resty-signal)
# NGX_SRC_BRANCH+=(master)

# NGX_SRC_PATH+=(resty/lua-cjson)
# NGX_SRC_REPO+=(openresty/lua-cjson)
# NGX_SRC_BRANCH+=(master)

# NGX_SRC_PATH+=(resty/lua-resty-shell)
# NGX_SRC_REPO+=(openresty/lua-resty-shell)
# NGX_SRC_BRANCH+=(master)

# NGX_SRC_PATH+=(lua/luaposix)
# NGX_SRC_REPO+=(luaposix/luaposix)
# NGX_SRC_BRANCH+=(master)
